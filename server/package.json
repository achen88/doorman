{
  "name": "doorman-server",
  "version": "1.0.0",
  "description": "Server to handle Twilio webhooks and communicate with ESP8266 via MQTT.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mqtt": "^4.2.8",
    "twilio": "^3.71.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
